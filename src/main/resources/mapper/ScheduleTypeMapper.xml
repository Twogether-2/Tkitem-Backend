<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="tkitem.backend.domain.scheduleType.mapper.ScheduleTypeMapper">
    <select id="selectAll" resultType="tkitem.backend.domain.scheduleType.vo.ScheduleType">
        SELECT schedule_type_id as scheduleTypeId, name as name, name_kr as nameKr
        FROM SCHEDULE_TYPE
    </select>
</mapper>