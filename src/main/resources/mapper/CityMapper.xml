<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="tkitem.backend.domain.city.mapper.CityMapper">
    <insert id="save" parameterType="tkitem.backend.domain.city.vo.City">
        INSERT INTO CITY (country_name, city_name, created_at, updated_at)
        VALUES(#{countryName}, #{cityName}, SYSTIMESTAMP, SYSTIMESTAMP)
    </insert>

    <select id="findCitiesByName" resultType="tkitem.backend.domain.city.vo.City">
        SELECT city_id, city_name, country_name
        FROM city
        WHERE city_name IN
        <foreach collection="names" item="name" open="(" separator="," close=")">
            #{name}
        </foreach>
    </select>

    <select id="findCityIdByName" resultType="long" parameterType="String">
        SELECT city_id
        FROM city
        WHERE city_name = #{name} and country_name = #{countryName}
    </select>
</mapper>
