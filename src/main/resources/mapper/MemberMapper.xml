<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="tkitem.backend.domain.member.mapper.MemberMapper">
    <!-- 회원 가입 -->
    <insert id="insertMember" parameterType="tkitem.backend.domain.member.vo.SignUp">
        <selectKey keyProperty="id" resultType="long" order="BEFORE">
            SELECT MEMBER_SEQ.NEXTVAL FROM DUAL
        </selectKey>
        INSERT INTO MEMBER (
            MEMBER_ID,
            EMAIL,
            PASSWORD,
            NICKNAME,
            ROLE,
            BIRTHDAY,
            GENDER,
            CREATED_BY,
            UPDATED_BY
        ) VALUES (
            #{memberId},
            #{email},
            #{password},
            #{nickname},
            'ROLE_USER',
            TO_DATE(#{birthday}, 'YYYY-MM-DD'),
            #{gender},
            #{memberId},
            #{memberId}
        )
    </insert>

    <!-- 회원 조회 -->
    <select id="selectMemberByMemberId" parameterType="long" resultType="tkitem.backend.domain.member.vo.Member">
        SELECT
            MEMBER_ID as memberId,
            EMAIL,
            PASSWORD,
            NICKNAME,
            ROLE,
            TO_CHAR(BIRTHDAY, 'YYYY-MM-DD') AS birthday,
            IS_DELETED AS isDeleted,
            GENDER,
            IMG_URL AS imgUrl,
            UPDATED_AT AS updatedAt
        FROM MEMBER
        WHERE MEMBER_ID = #{memberId} AND IS_DELETED = 'F'
    </select>

    <!-- 이메일로 회원 조회 -->
    <select id="selectMemberByEmail" resultType="tkitem.backend.domain.member.vo.Member">
        SELECT
        MEMBER_ID as memberId,
        EMAIL,
        PASSWORD,
        NICKNAME,
        ROLE,
        TO_CHAR(BIRTHDAY, 'YYYY-MM-DD') AS birthday,
        IS_DELETED AS isDeleted,
        GENDER,
        IMG_URL AS imgUrl,
        UPDATED_AT AS updatedAt
        FROM MEMBER
        WHERE EMAIL = #{email} AND IS_DELETED = 'F'
    </select>

    <!-- 이메일과 타입, oauthId로 회원 조회 -->
    <select id="selectMemberByEmailAndTypeAndOauthId" resultType="tkitem.backend.domain.member.vo.Member">
        SELECT
        MEMBER_ID AS memberId,
        EMAIL,
        PASSWORD,
        NICKNAME,
        ROLE,
        TO_CHAR(BIRTHDAY, 'YYYY-MM-DD') AS birthday,
        IS_DELETED AS isDeleted,
        GENDER,
        IMG_URL AS imgUrl,
        TYPE,
        UPDATED_AT AS updatedAt
        FROM MEMBER
        JOIN SOCIAL_TYPE ON MEMBER.MEMBER_ID = SOCIAL_TYPE.MEMBER_ID
        WHERE EMAIL = #{email}
        AND SOCIAL_TYPE.TYPE = #{type}
        AND IS_DELETED = 'F'
        <if test="oauthId != null">
            AND OAUTH_ID = #{oauthId}
        </if>
    </select>

    <!-- 이메일 존재 여부 -->
    <select id="existsByEmail" resultType="boolean">
        SELECT CASE WHEN COUNT(*) > 0 THEN 1 ELSE 0 END
        FROM MEMBER
        WHERE EMAIL = #{email}
    </select>

    <!-- 일반 로그인 시 이메일 + 비밀번호 존재 여부 -->
    <select id="existsByEmailAndPassword" resultType="boolean">
        SELECT CASE WHEN COUNT(*) > 0 THEN 1 ELSE 0 END
        FROM MEMBER
        JOIN SOCIAL_TYPE ON MEMBER.MEMBER_ID = SOCIAL_TYPE.MEMBER_ID
        WHERE
        EMAIL = #{email}
        AND PASSWORD = #{password}
        AND IS_DELETED = 'F'
        AND SOCIAL_TYPE.TYPE = 'NONE'
    </select>

    <!-- 이메일 + 소셜타입 존재 여부 -->
    <select id="existsByEmailAndSocialType" resultType="boolean">
        SELECT CASE WHEN COUNT(*) > 0 THEN 1 ELSE 0 END
        FROM MEMBER
        JOIN SOCIAL_TYPE ON MEMBER.MEMBER_ID = SOCIAL_TYPE.MEMBER_ID
        WHERE
        EMAIL = #{email}
        AND SOCIAL_TYPE.TYPE = #{type}
        AND OAUTH_ID = #{oauthId}
    </select>

    <!-- 회원 탈퇴   -->
    <update id="updateIsDeleted" parameterType="long">
        UPDATE MEMBER
        SET IS_DELETED = 'T', UPDATED_AT = SYSDATE, UPDATED_BY = #{memberId}
        WHERE MEMBER_ID = #{memberId}
    </update>

    <!-- 회원 재가입  -->
    <update id="updateIsDeletedFalse">
        UPDATE MEMBER
        SET IS_DELETED = 'F', UPDATED_AT = SYSDATE
        WHERE IS_DELETED = 'T' AND EMAIL = #{email}
    </update>
</mapper>


